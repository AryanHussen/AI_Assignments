// This import extends the 'expect' library from Jest.
// It allows you to write tests that check the UI, like:
// expect(button).toBeDisabled() or expect(header).toBeInTheDocument()
import '@testing-library/jest-dom';
