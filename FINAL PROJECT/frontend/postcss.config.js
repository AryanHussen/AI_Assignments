export default {
  // Plugins are tools that process your CSS
  plugins: {
    // This plugin enables Tailwind CSS to process your utility classes
    tailwindcss: {},
    
    // This plugin automatically adds "vendor prefixes" (like -webkit- or -moz-) 
    // to your CSS so that styles look the same on all browsers (Chrome, Safari, Firefox)
    autoprefixer: {},
  },
}
